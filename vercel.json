{
  "builds": [
    {
      "src": "server/app.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": [
          "server/public/**",
          "server/static/**",
          "server/*.py",
          "models.py",
          "research/**",
          "mcp/**",
          "memory/**"
        ]
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "server/app.py"
    }
  ]
}